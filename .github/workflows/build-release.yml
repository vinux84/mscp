name: Build & Release

on:
  push:
    tags:
      - 'v*'

# Least privilege: read-only by default. Only the release job elevates to write.
permissions:
  contents: read

jobs:
  # ── Generate build matrix from plugins.json ──
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.gen.outputs.matrix }}
      names: ${{ steps.gen.outputs.names }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - id: gen
        shell: pwsh
        run: |
          $plugins = Get-Content plugins.json -Raw | ConvertFrom-Json
          $include = @()
          $names = @()

          foreach ($p in $plugins) {
            $project = if ($p.project) { $p.project } else { "$($p.name).csproj" }
            $platform = if ($p.platform) { $p.platform } else { 'AnyCPU' }
            $outputPath = if ($p.outputPath) { $p.outputPath }
                          elseif ($platform -eq 'x64') { 'bin/x64/Release/net48' }
                          else { 'bin/Release/net48' }

            # Build project list (main + extras)
            $projects = @("$($p.path)/$project" -replace '/', '\')
            if ($p.extraProjects) {
              foreach ($ep in $p.extraProjects) {
                $projects += ($ep -replace '/', '\')
              }
            }

            $include += @{
              name       = $p.name
              projects   = $projects -join ';'
              platform   = $platform
              stage_from = ("$($p.path)/$outputPath" -replace '/', '\')
              stage_to   = $p.name
            }
            $names += $p.name
          }

          $matrix = @{ include = $include } | ConvertTo-Json -Depth 5 -Compress
          $namesList = $names | ConvertTo-Json -Compress

          "matrix=$matrix" >> $env:GITHUB_OUTPUT
          "names=$namesList" >> $env:GITHUB_OUTPUT

  # ── Matrix build: each plugin builds in parallel ──
  build:
    needs: setup
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.setup.outputs.matrix) }}

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3.0.0

      - name: Setup NuGet
        uses: NuGet/setup-nuget@b26b823c478ee115be5c9403e62c90b0bf943843 # v3.1.0

      - name: Restore NuGet packages
        run: nuget restore MSCPlugins.sln

      - name: Build ${{ matrix.name }}
        run: |
          $projects = "${{ matrix.projects }}" -split ";"
          foreach ($proj in $projects) {
            msbuild $proj /p:Configuration=Release /p:Platform="${{ matrix.platform }}" /p:CIBuild=true
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
        shell: pwsh

      # The PKI plugin ships the standalone single-file Cert Installer
      # EXE alongside it (used by the "Save Cert Installer..." button on
      # the Overview node, and bundled in PKI.zip). Publish it BEFORE the
      # Extra staging step so plugins.json's extraStagingFiles entry
      # resolves to a real file.
      - name: Publish PKI Cert Installer (PKI matrix entry only)
        if: matrix.name == 'PKI'
        shell: pwsh
        run: |
          dotnet publish Installer\Mscp.PkiCertInstaller\Mscp.PkiCertInstaller.csproj `
            -c Release -r win-x64 --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -p:CIBuild=true /v:minimal /nologo
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Stage ${{ matrix.name }}
        run: |
          New-Item -ItemType Directory -Path staging\${{ matrix.stage_to }} -Force
          Copy-Item -Path "${{ matrix.stage_from }}\*" -Destination staging\${{ matrix.stage_to }} -Recurse
        shell: pwsh

      - name: Extra staging for ${{ matrix.name }}
        shell: pwsh
        run: |
          $plugins = Get-Content plugins.json -Raw | ConvertFrom-Json
          $p = $plugins | Where-Object { $_.name -eq '${{ matrix.name }}' }
          if ($p.extraStagingDirs) {
            foreach ($dir in $p.extraStagingDirs) {
              $src = $dir -replace '/', '\'
              Copy-Item -Path "$src\*" -Destination "staging\${{ matrix.stage_to }}" -Recurse -Force
              Write-Host "Extra staging dir: $src"
            }
          }
          if ($p.extraStagingFiles) {
            foreach ($file in $p.extraStagingFiles) {
              $src = $file -replace '/', '\'
              Copy-Item -Path $src -Destination "staging\${{ matrix.stage_to }}" -Force
              Write-Host "Extra staging file: $src"
            }
          }

      - name: Create ZIP
        run: Compress-Archive -Path staging\${{ matrix.stage_to }} -DestinationPath ${{ matrix.name }}-${{ github.ref_name }}.zip
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}-${{ github.ref_name }}.zip
          # Intermediate build artifact; the release assets live on the
          # GitHub Release, so these can expire quickly.
          retention-days: 1

  # ── Collect all artifacts, build unified installer, create release ──
  release:
    needs: [setup, build]
    runs-on: windows-latest
    # This job publishes the release and attests build provenance.
    permissions:
      contents: write      # publish the GitHub Release
      id-token: write      # OIDC for keyless signing (Sigstore)
      attestations: write  # store the build provenance attestation

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3.0.0

      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts

      - name: Install WiX Toolset
        shell: pwsh
        run: |
          dotnet tool install --global wix --version 5.0.2
          wix extension add WixToolset.UI.wixext/5 -g
          wix extension add WixToolset.Util.wixext/5 -g
          wix extension add WixToolset.Iis.wixext/5.0.2 -g

      - name: Generate WiX components
        shell: pwsh
        run: |
          pwsh installer/generate-wix.ps1

      - name: Build installer custom actions
        shell: pwsh
        run: |
          msbuild installer\customactions\InstallerCustomActions.csproj `
            /restore `
            /p:Configuration=Release `
            /t:Rebuild `
            /v:minimal

      - name: Stage IIS hosting payload
        shell: pwsh
        run: |
          $version = '${{ github.ref_name }}' -replace '^v',''
          $publicStaged = Join-Path (Get-Location) 'public-staged'
          pwsh installer/stage-iis-hosting.ps1 `
            -ManifestPath plugins.json `
            -PublicSrc installer/public `
            -PublicStaged $publicStaged `
            -Version $version

      - name: Collect per-plugin ZIPs for WiX bindpath
        shell: pwsh
        run: |
          # Components.wxs references each plugin ZIP via !(bindpath.zips). The
          # CI artifacts land under artifacts/<name>/<name>-v<version>.zip;
          # collect them into one flat folder so wix can resolve the source.
          $zipsRoot = Join-Path (Get-Location) 'zips-flat'
          if (Test-Path $zipsRoot) { Remove-Item $zipsRoot -Recurse -Force }
          New-Item -ItemType Directory -Path $zipsRoot -Force | Out-Null
          Get-ChildItem 'artifacts' -Recurse -Filter '*.zip' | ForEach-Object {
            Copy-Item $_.FullName -Destination $zipsRoot -Force
          }

      - name: Stage PKI Cert Installer EXE (versioned) for WiX
        shell: pwsh
        run: |
          # IisHosting.wxs has a <File Source="!(bindpath.zips)\Mscp.PkiCertInstaller-v<Version>.exe" />
          # that ships the standalone EXE on the local /mscp/ download
          # page. The PKI matrix step bundled the EXE inside PKI.zip;
          # pull it back out, rename to the versioned filename, drop
          # next to the per-plugin ZIPs so wix can resolve the source.
          $version = '${{ github.ref_name }}' -replace '^v',''
          $zipsRoot = Join-Path (Get-Location) 'zips-flat'
          $pkiZip = Get-ChildItem 'artifacts\PKI' -Filter 'PKI*.zip' -Recurse | Select-Object -First 1
          if (-not $pkiZip) { throw "PKI artifact not found under artifacts\PKI" }
          $tmp = Join-Path $env:RUNNER_TEMP 'pki-extract'
          if (Test-Path $tmp) { Remove-Item $tmp -Recurse -Force }
          New-Item -ItemType Directory -Path $tmp -Force | Out-Null
          Expand-Archive -Path $pkiZip.FullName -DestinationPath $tmp -Force
          $exe = Get-ChildItem $tmp -Filter 'Mscp.PkiCertInstaller.exe' -Recurse | Select-Object -First 1
          if (-not $exe) { throw "Mscp.PkiCertInstaller.exe not found inside $($pkiZip.Name)" }
          Copy-Item $exe.FullName -Destination (Join-Path $zipsRoot "Mscp.PkiCertInstaller-v$version.exe") -Force
          Write-Host "Staged Mscp.PkiCertInstaller-v$version.exe ($([math]::Round($exe.Length/1MB,1)) MB)"

      - name: Build MSI installer
        shell: pwsh
        run: |
          $version = '${{ github.ref_name }}' -replace '^v',''
          $publicStaged = Join-Path (Get-Location) 'public-staged'
          $zipsRoot = Join-Path (Get-Location) 'zips-flat'
          wix build installer/wix/Product.wxs installer/wix/Components.wxs installer/wix/IisHosting.wxs `
            -ext WixToolset.UI.wixext `
            -ext WixToolset.Util.wixext `
            -ext WixToolset.Iis.wixext `
            -d Version=$version `
            -arch x64 `
            -b installer/wix `
            -bindpath "publicroot=$publicStaged" `
            -bindpath "zips=$zipsRoot" `
            -pdbtype none `
            -o "MSCPlugins-$version-Setup.msi"

      - name: Collect release files
        shell: pwsh
        run: |
          $names = '${{ needs.setup.outputs.names }}' | ConvertFrom-Json
          foreach ($name in $names) {
            Copy-Item "artifacts\$name\*.zip" -Destination .
          }

      # Generate a signed SLSA provenance attestation for the installer and
      # ZIPs. Verifiable later with: gh attestation verify <file> -R Cacsjep/mscp
      - name: Attest build provenance
        uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2
        with:
          subject-path: |
            MSCPlugins-*-Setup.msi
            *-${{ github.ref_name }}.zip

      # SHA-256 checksums of every release artifact, signed with cosign
      # (keyless, using the job's OIDC token). Scorecard's Signed-Releases
      # check looks for a signature asset on the release; checksums.txt.sig
      # satisfies it, and users can verify their downloads against it.
      - name: Generate SHA-256 checksums
        shell: pwsh
        run: |
          $files = Get-ChildItem -File | Where-Object {
            $_.Name -like "*-${{ github.ref_name }}.zip" -or $_.Name -like "MSCPlugins-*-Setup.msi"
          }
          $lines = foreach ($f in ($files | Sort-Object Name)) {
            $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $f.FullName).Hash.ToLower()
            "$hash  $($f.Name)"
          }
          $lines | Set-Content -Encoding ascii checksums.txt
          Get-Content checksums.txt

      - name: Install cosign
        uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1

      - name: Sign checksums with cosign (keyless)
        shell: pwsh
        run: |
          cosign sign-blob --yes checksums.txt `
            --output-signature checksums.txt.sig `
            --output-certificate checksums.txt.pem
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          files: |
            *-${{ github.ref_name }}.zip
            MSCPlugins-*-Setup.msi
            checksums.txt
            checksums.txt.sig
            checksums.txt.pem
          generate_release_notes: true
